# Test: No files match pattern (should warn but continue)
logfile /tmp/rinetd-test.log

0.0.0.0 8601/tcp 127.0.0.1 9601/tcp

# This pattern should match no files - should log warning and continue
include nonexistent/*.conf

# This rule should still be parsed
0.0.0.0 8602/tcp 127.0.0.1 9602/tcp
